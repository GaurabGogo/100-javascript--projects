// const buttonClk = document.querySelector("#btn");

// buttonClk.addEventListener("click", function (e) {
//   Notification.requestPermission().then((perm) => {
//     if (perm === "granted") {
//       const notification = new Notification("Example Notification", {});
//       var audio = new Audio(
//         "http://commondatastorage.googleapis.com/codeskulptor-assets/Evillaugh.ogg"
//       );

//       notification.addEventListener("show", (e) => audio.play());
//       notification.addEventListener("close", (e) => audio.pause());
//     }
//   });
// });